<!DOCTYPE html>
<html>
<head>
  <title>Edit User</title>
  <style>
    body { font-family: Arial; padding: 20px; background: #fafafa; }
    form { max-width: 400px; margin: 30px auto; background: white; padding: 20px; border-radius: 10px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
    input, button { width: 90%; padding: 10px; margin: 10px auto; display: block; }
  </style>
</head>
<body>

  <h2 style="text-align:center;">Edit User</h2>

  <form action="/users/edit/<%= user._id %>" method="POST">
    <input type="text" name="name" value="<%= user.name %>" required />
    <input type="email" name="email" value="<%= user.email %>" required />
    <button type="submit">Update</button>
  </form>

</body>
</html>
